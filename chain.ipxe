#! ipxe

echo Starting Herrah iPXE chain...
echo

# Init networking
dhcp

# Debug info
echo next-server: ${next-server}
echo filename	: ${filename}
echo MAC address: ${net0/mac}
echo IP address : ${ip}

# Chain new iPXE file
chain http://${next-server}/${net0/mac}
